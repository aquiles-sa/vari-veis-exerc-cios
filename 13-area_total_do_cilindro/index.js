// Atividade para calcular a área total de um cilíndro //

// At = 2piR(R + H)     (fórmula) //

let raio_da_base = 5; // raio 
let altura_do_cilindro = 1; // altura

let area_total_do_cilindro = 2 * raio_da_base * (raio_da_base + altura_do_cilindro);
// area total do cilindro = 2 * 5 * (5 + 1) //

console.log(area_total_do_cilindro + "pi");