// C = 2piR  (fórmula do comprimento) //
// A = pi * R(ao quadrado) (fórmula da área) //


// comprimento do círculo //
let raio = 3; // valor do raio
let comprimento = 2 * Math.PI * raio; // valor do comprimento
let message = "O valor do comprimento é:";
console.log(`${message} ${comprimento}`);

// Área do círculo //
let raio2 = Math.pow(3, 2); // 3 elevado ao quadrado 
let area = Math.PI * raio2; // resultado da área
let message2 = "O valor da área é:";
console.log(`${message} ${area}`);

